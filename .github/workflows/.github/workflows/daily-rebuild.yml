name: Daily Rebuild

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Call Cloudflare deploy hook
        run: curl -X POST ${{ secrets.CF_DEPLOY_HOOK }}
